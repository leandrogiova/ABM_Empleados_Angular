import { BuscarEmpleadoPipe } from './buscar-empleado.pipe';

describe('BuscarEmpleadoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarEmpleadoPipe();
    expect(pipe).toBeTruthy();
  });
});
